# Template only — no real secrets here. Never commit the real `.dev.vars` file.
# The Deploy to Cloudflare setup page reads this file to prompt for the ADMIN password.
ADMIN=change-me-to-a-strong-password
